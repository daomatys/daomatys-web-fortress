footer.footer
  .footer__proposal 
    span ...so
    span.footer__changeable-word  {{changeableWord}}
    span  me, maybe ?
  .footer__social-icons
    ng-container( *ngFor="let socialIcon of socialIcons" )
      app-social-icon(
        [socialIcon]="socialIcon"
        "(onMouseEnterTheFooterIcon)"="onMouseEnterEvent( $event )"
      )
